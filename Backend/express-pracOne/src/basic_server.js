
const http = require('http')

const server = http.createServer((req, res)=>{
    // req.method  := which method is used eg : GET, POST
    // req.url := req came from which route eg: /users , /about
    // req.
})